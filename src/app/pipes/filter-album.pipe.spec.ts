import { FilterAlbumPipe } from './filter-album.pipe';

describe('FilterAlbumPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAlbumPipe();
    expect(pipe).toBeTruthy();
  });
});
